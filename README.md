# USPAS 2026 Jupyter helpers

Helper modules and instructor/student notebooks for the USPAS 2026 accelerator physics labs.

## Colab setup

Each notebook installs the helper modules from this repository before importing them:

```python
HELPER_VERSION = "main"
HELPER_REPO = "git+https://github.com/r-hensley/uspas-2026-jupyter.git"

import subprocess
import sys

subprocess.check_call([
    sys.executable,
    "-m",
    "pip",
    "install",
    "--quiet",
    "--upgrade",
    f"{HELPER_REPO}@{HELPER_VERSION}",
])
```

For a student release, replace `main` with a tag such as `v2026-lab1` after pushing the matching helper code.

## Helper modules

- `dc_local_helpers.py` supports the dispersion and chromaticity lab.
- `quadrupole_focusing_xsuite_helpers.py` supports the quadrupole focusing Xsuite lab.

## Notebook workflow

Instructor solution notebooks are the source of truth. Student notebooks are generated by removing solution-tagged cells and clearing outputs:

```bash
python3 make_student_versions.py
python3 check_student_versions.py
```
